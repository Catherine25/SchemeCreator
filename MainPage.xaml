<Page
    x:Class="SchemeCreator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchemeCreator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="MainPageName"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Frame Name="MainFrame">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <!-- for buttons-->
                <RowDefinition Height="15*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Background="DarkCyan" x:Name="NewSchemeBt" Content="New Scheme" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top"/>
            <Button Grid.Column="1" Grid.Row="0" Background="DarkCyan" x:Name="NewElementBt" Content="New Element" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top" Margin="120,0"/>
            <Button Grid.Column="2" Grid.Row="0" Background="DarkCyan" x:Name="NewLineBt" Content="New Line" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top" Margin="240,0"/>
            <Button Grid.Column="3" Grid.Row="0" Background="DarkCyan" x:Name="ChangeValueBt" Content="Change Value" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top" Margin="360,0"/>
            <Button Grid.Column="4" Grid.Row="0" Background="DarkCyan" x:Name="WorkBt" Content="Work" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top" Margin="480,0"/>
            <Button Grid.Column="5" Grid.Row="0" Background="DarkCyan" x:Name="TraceBt" Content="Trace" Height="50" Width="110" Grid.RowSpan="2" VerticalAlignment="Top" Margin="600,0"/>

            <Grid x:Name="WorkSpace" Grid.Row="1" HorizontalAlignment="Stretch" Margin="2,0.333,-2,0" VerticalAlignment="Stretch"/>
        </Grid>
    </Frame>
</Page>